// src/error-handling/error-normalizer.ts

export class ErrorNormalizer {
  normalizeError(error: any): any {
    console.log('Normalizing error:', error);
    return error; // Placeholder error normalization
  }
}
